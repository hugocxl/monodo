{
  "name": "monodo",
  "displayName": "Monodo",
  "version": "0.0.0",
  "private": true,
  "description": "A minimal task management app",
  "keywords": [],
  "author": "Hugo Corta <corta.hugo@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r --parallel --filter=./{backend,frontend,shared}/** dev",
    "build-fast": "pnpm -r --parallel --filter=./{backend,frontend,shared}/** build-fast",
    "build": "pnpm -r --filter=./{backend,frontend,shared}/** build",
    "test": "vitest run",
    "test:dev": "vitest dev -u",
    "test:coverage": "vitest run --coverage",
    "clean": "pnpm -r --parallel exec rimraf node_modules dist && rimraf node_modules",
    "fix": "pnpm lint:fix . && pnpm format:fix .",
    "lint": "eslint --cache",
    "lint:check": "pnpm lint",
    "lint:fix": "pnpm lint --fix",
    "format": "prettier --cache",
    "format:check": "pnpm format --check",
    "format:fix": "pnpm format --write",
    "types": "tsc",
    "types:check": "pnpm types --noEmit",
    "prepare": "husky install",
    "heroku-postbuild": "cd backend && npm run start"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/passport-jwt": "^3",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@typescript-eslint/eslint-plugin": "^6",
    "@typescript-eslint/parser": "^6",
    "@vitest/coverage-v8": "^0.34.6",
    "dotenv": "^16",
    "eslint": "^8",
    "eslint-config-prettier": "^9",
    "husky": "^8",
    "lint-staged": "^14",
    "prettier": "^3",
    "rimraf": "^5",
    "tsc-files": "^1.1.4",
    "tsup": "^7",
    "typescript": "^5",
    "vitest": "^0"
  }
}
